/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('action.creep.move');
 * mod.thing == 'a thing'; // true
 */

module.exports = {
    run: function(creepObject, targetObject, mode) {
        
    },
    MOVE_AGGRESSIVE_STRATEGY: 1,
    MOVE_DIRECT_STRATEGY: 2,
    MOVE_SAFE_STRATEGY: 3,
};